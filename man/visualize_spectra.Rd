% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{visualize_spectra}
\alias{visualize_spectra}
\title{Visualize One or More Overlaid Mass Spectra}
\usage{
visualize_spectra(
  spectra,
  interest_range = NULL,
  xlim = NULL,
  ylim = NULL,
  main = NULL,
  cex.axis = 1,
  cex.lab = 1,
  cex.main = 1.3,
  lwd = 1.5,
  col_platte = c("Viridis", "YlOrRd")
)
}
\arguments{
\item{spectra}{A list of spectrum matrices/data frames, each with m/z in the
first column and intensity in the second.}

\item{interest_range}{Optional numeric vector giving an m/z range to restrict
the spectra to before plotting.}

\item{xlim}{Optional numeric vector of length two limiting the x-axis range.}

\item{ylim}{Optional numeric vector of length two limiting the y-axis range.}

\item{main}{Optional plot title.}

\item{cex.axis}{Axis annotation scaling factor.}

\item{cex.lab}{Axis label scaling factor.}

\item{cex.main}{Title scaling factor.}

\item{lwd}{Line width of the spectra traces.}

\item{col_platte}{Sequential \code{colorspace} palette used to color the traces.}
}
\value{
Invisibly \code{NULL}; called for the side effect of drawing a plot.
}
\description{
Draws one or more mass spectra as overlaid base-R line plots, colored by a
sequential palette. No legend is drawn.
}
\examples{
if (requireNamespace("colorspace", quietly = TRUE)) {
  x <- seq(1000, 2000, length.out = 1000)
  spectra <- list(
    sample_1 = data.frame(mz = x, intensity = dnorm(x, 1400, 25) * 100),
    sample_2 = data.frame(mz = x, intensity = dnorm(x, 1600, 25) * 80)
  )

  visualize_spectra(spectra, main = "Example spectra")
}

}
\seealso{
\code{\link[=visualize_spectrum]{visualize_spectrum()}}
}
