% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{visualize_spectrum}
\alias{visualize_spectrum}
\title{Visualize a Single Mass Spectrum with Optional Peak Segments}
\usage{
visualize_spectrum(
  spectrum,
  peaks = NULL,
  interest_range = NULL,
  annotate_topN = FALSE,
  topN = 10,
  xlim = NULL,
  ylim = NULL,
  main = NULL,
  cex.axis = 1,
  cex.lab = 1,
  cex.main = 1.3,
  lwd = 1,
  col = "black",
  peaks_lwd = 2,
  peaks_col = "red",
  peaks_lty = 3
)
}
\arguments{
\item{spectrum}{A numeric matrix or data frame with m/z in the first column
and intensity in the second column.}

\item{peaks}{Optional peak matrix or data frame. When supplied, vertical peak
segments are drawn from zero to each peak intensity.}

\item{interest_range}{Optional numeric vector giving an m/z range to restrict
the spectrum (and peaks) to before plotting.}

\item{annotate_topN}{Logical. When \code{TRUE}, the most intense peaks are labelled
with their m/z values. The default is \code{FALSE}.}

\item{topN}{Number of peaks to annotate when \code{annotate_topN = TRUE}.}

\item{xlim}{Optional numeric vector of length two limiting the x-axis range.}

\item{ylim}{Optional numeric vector of length two limiting the y-axis range.}

\item{main}{Optional plot title.}

\item{cex.axis}{Axis annotation scaling factor.}

\item{cex.lab}{Axis label scaling factor.}

\item{cex.main}{Title scaling factor.}

\item{lwd}{Line width of the spectrum trace.}

\item{col}{Line color of the spectrum trace.}

\item{peaks_lwd}{Line width of the peak segments.}

\item{peaks_col}{Color of the peak segments.}

\item{peaks_lty}{Line type of the peak segments.}
}
\value{
Invisibly \code{NULL}; called for the side effect of drawing a plot.
}
\description{
Draws a single mass spectrum as a base-R line plot. When a peak table is
supplied, vertical segments are drawn from zero to each peak intensity, with
an option to annotate the most intense peaks.
}
\examples{
x <- seq(1000, 2000, length.out = 1000)
y <- dnorm(x, mean = 1500, sd = 25) * 100
spectrum <- data.frame(mz = x, intensity = y)

visualize_spectrum(spectrum, main = "Example spectrum")

}
\seealso{
\code{\link[=visualize_spectra]{visualize_spectra()}}, \code{\link[=find_peaks]{find_peaks()}}
}
