% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_maldi.R
\name{plot_spectrum}
\alias{plot_spectrum}
\title{Plot a Single Mass Spectrum with Optional Peak Segments}
\usage{
plot_spectrum(spectrum, peaks = NULL, xlim = NULL, ylim = NULL, title = NULL)
}
\arguments{
\item{spectrum}{A numeric matrix or data frame with m/z in the first column
and intensity in the second column.}

\item{peaks}{Optional peak matrix or data frame. When supplied, vertical peak
segments are drawn from zero to each peak intensity.}

\item{xlim}{Optional numeric vector of length two limiting the x-axis range.}

\item{ylim}{Optional numeric vector of length two limiting the y-axis range.}

\item{title}{Optional plot title.}
}
\value{
A \code{ggplot2} object.
}
\description{
Draws a single mass spectrum as a line plot using \code{ggplot2}. When a peak
table is supplied, vertical segments are drawn from zero to each peak
intensity. Requires the suggested package \code{ggplot2}.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  x <- seq(1000, 2000, length.out = 1000)
  y <- dnorm(x, mean = 1500, sd = 25) * 100
  spectrum <- data.frame(mz = x, intensity = y)

  plot_spectrum(spectrum, title = "Example spectrum")
}

}
\seealso{
\code{\link[=plot_spectra]{plot_spectra()}}, \code{\link[=find_peaks]{find_peaks()}}
}
