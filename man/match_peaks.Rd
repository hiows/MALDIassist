% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match_peaks.R
\name{match_peaks}
\alias{match_peaks}
\title{Match Detected Peaks to Reference m/z Values}
\usage{
match_peaks(
  peaks,
  reference_mz,
  reference_names = NULL,
  hws_match = 10,
  peak_selection_mode = c("nearest_mz", "maximum_intensity")
)
}
\arguments{
\item{peaks}{A numeric matrix or data frame containing detected peaks.
The first column must contain peak positions, typically m/z values, and
the second column must contain the corresponding peak intensities.}

\item{reference_mz}{A numeric vector of reference m/z values to match
against.}

\item{reference_names}{A character vector of names corresponding to
\code{reference_mz}. If \code{NULL}, \code{NA} is used for all reference names. The
default is \code{NULL}.}

\item{hws_match}{A positive finite numeric scalar specifying the half-window
size used for matching. For each reference m/z, only peaks within
\verb{[reference_mz - hws_match, reference_mz + hws_match]} are considered.
The default is \code{10}.}

\item{peak_selection_mode}{Character string specifying how a peak is chosen
when multiple peaks fall within the matching window. Either
\code{"nearest_mz"} (default) or \code{"maximum_intensity"}.}
}
\value{
A data frame with one row per reference m/z, sorted in ascending
order of \code{reference_mz}. Columns are:
\itemize{
\item \code{reference_name}: reference label from \code{reference_names}, or \code{NA}.
\item \code{reference_mz}: reference m/z value.
\item \code{is_matched}: logical flag indicating whether a peak was found in the
matching window.
\item \code{detected_mz}: m/z of the matched peak, or \code{NA} when unmatched.
\item \code{detected_intensity}: intensity of the matched peak, or \code{NA} when
unmatched.
\item \code{delta_mz}: signed m/z difference, \code{detected_mz - reference_mz}, or
\code{NA} when unmatched.
}
}
\description{
For each reference m/z value, searches detected peaks within a symmetric
half-window and selects one peak per reference. Returns one row per reference
m/z with match status and detected peak properties.
}
\details{
When multiple peaks fall within the matching window, selection depends on
\code{peak_selection_mode}:
\itemize{
\item \code{"nearest_mz"}: the peak whose m/z is closest to \code{reference_mz}.
\item \code{"maximum_intensity"}: the peak with the highest intensity in the
second column of \code{peaks}.
}

If \code{peaks} is not sorted by m/z, it is sorted internally before matching.
}
\examples{
peaks <- data.frame(
  mz = c(1000, 2000.5, 3000),
  intensity = c(10, 50, 20)
)

match_peaks(
  peaks = peaks,
  reference_mz = c(999, 2005, 4000),
  reference_names = c("A", "B", "C"),
  hws_match = 10
)

}
