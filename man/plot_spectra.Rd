% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_maldi.R
\name{plot_spectra}
\alias{plot_spectra}
\title{Plot One or More Mass Spectra}
\usage{
plot_spectra(spectra, xlim = NULL, ylim = NULL, title = NULL)
}
\arguments{
\item{spectra}{A spectrum matrix/data frame or named list of spectra.}

\item{xlim}{Optional numeric vector of length two limiting the x-axis range.}

\item{ylim}{Optional numeric vector of length two limiting the y-axis range.}

\item{title}{Optional plot title.}
}
\value{
A \code{ggplot2} object.
}
\description{
Draws one or more mass spectra as overlaid line plots, colored by sample.
Accepts a single spectrum or a named list of spectra. Requires the suggested
package \code{ggplot2}.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  x <- seq(1000, 2000, length.out = 1000)
  spectra <- list(
    sample_1 = data.frame(mz = x, intensity = dnorm(x, 1400, 25) * 100),
    sample_2 = data.frame(mz = x, intensity = dnorm(x, 1600, 25) * 80)
  )

  plot_spectra(spectra, title = "Example spectra")
}

}
\seealso{
\code{\link[=plot_spectrum]{plot_spectrum()}}
}
