% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_significance.R
\name{estimate_significance}
\alias{estimate_significance}
\title{Estimate Significant m/z Features Between Two Groups}
\usage{
estimate_significance(
  matched_matrix,
  group,
  feat_names = NULL,
  stat_method = c("t.test", "wilcox"),
  adj_method = c("none", "BH", "bonferroni")
)
}
\arguments{
\item{matched_matrix}{A numeric matrix or data frame with samples in rows and
features (m/z markers) in columns. A data frame is coerced with
\code{as.matrix()} and must contain only numeric columns.}

\item{group}{A vector of group labels with one entry per row of
\code{matched_matrix}. It must contain exactly two distinct (non-\code{NA}) levels.
The first level in factor order is treated as the reference group.}

\item{feat_names}{Optional character vector of feature names, one per column
of \code{matched_matrix}. If \code{NULL}, the column names of \code{matched_matrix} are
used when available, otherwise \code{paste0("feat_", seq_len(ncol))}.}

\item{stat_method}{The statistical test to apply per feature, either
\code{"t.test"} (Welch two-sample t-test) or \code{"wilcox"} (Wilcoxon rank-sum test
with \code{exact = FALSE}). The default is \code{"t.test"}.}

\item{adj_method}{The multiple-comparison adjustment passed to
\code{\link[stats:p.adjust]{stats::p.adjust()}}, one of \code{"none"}, \code{"BH"}, or \code{"bonferroni"}. The
default is \code{"none"}.}
}
\value{
A data frame with one row per feature and columns:
\itemize{
\item \code{feat_names}: feature name.
\item \code{pvalue}: raw two-sided p-value.
\item \code{adj_pvalue}: p-value after \code{adj_method} adjustment.
}
}
\description{
Performs a per-feature two-group comparison on a sample-by-feature matrix,
such as the intensity or detection matrix produced by
\code{\link[=build_matched_matrix]{build_matched_matrix()}}. For each feature (column), a two-sided test is run
between the two sample groups and the resulting p-values are optionally
adjusted for multiple comparisons.
}
\details{
The two group levels are determined from \code{as.factor(group)}. Rows are split
into the reference group (the first factor level) and the other group, and a
two-sided test is applied to each feature column:
\itemize{
\item For \code{"t.test"}, if both groups are constant for a feature (zero standard
deviation), the p-value is set to \code{NaN}, matching the behavior of a
degenerate comparison.
\item Tests that fail (for example, on constant data) return \code{NaN} for that
feature instead of raising an error, so a single problematic feature does
not abort the whole scan.
}
}
\examples{
set.seed(1)
n_per_group <- 10
matched_matrix <- rbind(
  matrix(rnorm(n_per_group * 3, mean = 0), nrow = n_per_group),
  matrix(rnorm(n_per_group * 3, mean = 1), nrow = n_per_group)
)
colnames(matched_matrix) <- paste0("mz_", c(1000, 2000, 3000))
group <- rep(c("control", "case"), each = n_per_group)

estimate_significance(
  matched_matrix = matched_matrix,
  group = group,
  stat_method = "t.test",
  adj_method = "BH"
)

}
\seealso{
\code{\link[=build_matched_matrix]{build_matched_matrix()}}, \code{\link[stats:p.adjust]{stats::p.adjust()}}
}
