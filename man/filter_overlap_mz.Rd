% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_overlap_mz.R
\name{filter_overlap_mz}
\alias{filter_overlap_mz}
\title{Filter Overlapping Frequent m/z Features}
\usage{
filter_overlap_mz(freq_data, hws_selection = 5)
}
\arguments{
\item{freq_data}{A data frame or matrix containing at least the columns
\code{mz} and \code{freq_ratio}, typically the output of \code{\link[=find_frequent_mz]{find_frequent_mz()}}.
Additional columns are preserved in the returned object.}

\item{hws_selection}{A non-negative finite numeric scalar specifying the
half-window size, in m/z units, used to define the local neighborhood for
each candidate. The default is \code{5}.}
}
\value{
An object of the same class as \code{freq_data}, containing only the
non-overlapping frequent m/z rows. If no rows remain, an empty object with
the same columns is returned.
}
\description{
Removes redundant frequent m/z values that fall within a local half-window
of each other. For every candidate, the function retains the row only when
its \code{freq_ratio} equals the maximum \code{freq_ratio} among all candidates in
\verb{[mz - hws_selection, mz + hws_selection]}. This is a post-processing step
for tables returned by \code{\link[=find_frequent_mz]{find_frequent_mz()}}, which can otherwise report
nearby m/z values (for example, differing by only about 1 Da) as separate
features.
}
\details{
Ties are retained: if two or more candidates share the same maximum
\code{freq_ratio} inside a neighborhood, all of them are kept.

The returned table keeps the original row order of retained candidates.
Row names are reset.
}
\examples{
freq_data <- data.frame(
  mz = c(3000, 3001, 5000, 5010),
  median_intensity = c(2.0, 1.8, 2.5, 2.1),
  count = c(18, 10, 20, 12),
  freq_ratio = c(0.9, 0.5, 1.0, 0.6)
)

filter_overlap_mz(freq_data, hws_selection = 5)

}
\seealso{
\code{\link[=find_frequent_mz]{find_frequent_mz()}}, \code{\link[=build_matched_matrix]{build_matched_matrix()}}
}
