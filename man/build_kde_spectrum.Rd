% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_kde_spectrum.R
\name{build_kde_spectrum}
\alias{build_kde_spectrum}
\alias{build_kde_spectra}
\title{Build a Gaussian KDE-Smoothed Spectrum}
\usage{
build_kde_spectrum(spectrum, bw = stats::median(diff(spectrum[, 1])))

build_kde_spectra(spectra, bw = NULL, n_cores = 1L)
}
\arguments{
\item{spectrum}{A numeric matrix or data frame with at least two columns.
The first column must contain strictly increasing x-axis values, typically
m/z values, and the second column must contain the corresponding
intensities.}

\item{bw}{A positive numeric scalar specifying the bandwidth of the Gaussian
KDE. The default is the median interval between adjacent x-axis values.}

\item{spectra}{A two-column \code{data.frame} or numeric \code{matrix} representing a
single spectrum, or a \code{list} of such objects. The first column must contain
strictly increasing x-axis values and the second column must contain
intensity values.}

\item{n_cores}{A positive integer specifying the number of worker processes
used when \code{spectra} is a list. The default is \code{1L}, which preserves
sequential behaviour.}
}
\value{
A list with three elements:
\itemize{
\item \code{spectrum}: A two-column \code{data.frame} containing the observed x-axis values
and the KDE-smoothed intensities. The column names are inherited from
\code{spectrum}.
\item \code{gauss_kde}: The Gaussian KDE closure returned by \code{\link[=get_gauss_kde]{get_gauss_kde()}}, which
can be re-evaluated at arbitrary x-axis values.
\item \code{bw}: The bandwidth used to build the KDE.
}

\code{build_kde_spectrum()} returns a single KDE-spectrum list.
\code{build_kde_spectra()} returns a single KDE-spectrum list when \code{spectra} is a
single spectrum, or a named list of KDE-spectrum lists with the same list
structure and names as \code{spectra}.
}
\description{
Builds a Gaussian kernel density estimate (KDE) representation of a mass
spectrum. The returned object contains the KDE-smoothed spectrum evaluated on
the observed m/z grid, together with the reusable KDE closure and the
bandwidth used. This prepares data that matches the Gaussian KDE-based peak
detection performed by \code{\link[=find_peaks]{find_peaks()}} and \code{\link[=find_peaks_spectra]{find_peaks_spectra()}}.
}
\details{
The KDE closure is created with \code{\link[=get_gauss_kde]{get_gauss_kde()}} using derivative order
\code{0}. The smoothed spectrum is obtained by evaluating this closure on the
observed x-axis grid, so the output spectrum has the same x-axis values as
the input. The column names of the input spectrum are preserved in the
output spectrum. When the input has no usable column names, \code{x} and \code{y} are
used as a fallback.

\code{build_kde_spectra()} applies \code{\link[=build_kde_spectrum]{build_kde_spectrum()}} to a single spectrum or
to each spectrum in a list. When \code{bw} is \code{NULL}, the per-spectrum default
bandwidth of \code{\link[=build_kde_spectrum]{build_kde_spectrum()}} (the median interval between adjacent
x-axis values) is used for every spectrum. Supplying a non-\code{NULL} \code{bw}
applies the same bandwidth to all spectra.
}
\examples{
x <- seq(1000, 2000, length.out = 2000)
y <- dnorm(x, mean = 1400, sd = 20) * 100 +
     dnorm(x, mean = 1700, sd = 25) * 80
spectrum <- data.frame(mz = x, intensity = y)

kde_spectrum <- build_kde_spectrum(spectrum, bw = 1)

head(kde_spectrum$spectrum)
kde_spectrum$gauss_kde(c(1400, 1700))

\dontrun{
spectra <- list(sample_1 = spectrum_1, sample_2 = spectrum_2)

kde_spectra <- build_kde_spectra(
  spectra = spectra,
  bw = 1,
  n_cores = 2
)
}

}
\seealso{
\code{\link[=get_gauss_kde]{get_gauss_kde()}}, \code{\link[=find_peaks]{find_peaks()}}, \code{\link[=find_peaks_spectra]{find_peaks_spectra()}}
}
