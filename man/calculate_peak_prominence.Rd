% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_peak_prominence.R
\name{calculate_peak_prominence}
\alias{calculate_peak_prominence}
\title{Calculate Peak Prominence}
\usage{
calculate_peak_prominence(
  data,
  peaks,
  valley_type = c("higher", "lower"),
  zero_tol = sqrt(.Machine$double.eps)
)
}
\arguments{
\item{data}{A data frame or matrix with at least two columns. The first
column must be x values, such as m/z, and the second column must be
intensity values.}

\item{peaks}{A data frame or matrix with at least two columns. The first
column must be peak x positions and the second column must be peak
intensities. Only the first two columns are used.}

\item{valley_type}{Character string specifying which reference valley should
be used. One of \code{"higher"} or \code{"lower"}. Default is \code{"higher"}.}

\item{zero_tol}{A single non-negative finite numeric value. Prominence values
with absolute value smaller than or equal to \code{zero_tol} are treated as zero.
Default is \code{sqrt(.Machine$double.eps)}.}
}
\value{
A numeric vector of peak prominence values. The returned vector has
the same length as the number of input peaks.
}
\description{
Calculate peak prominence for detected peaks.

This function validates spectrum data and detected peaks, then calls the
Rcpp backend \code{cpp_calculate_peak_prominence()}. Peak prominence is calculated
using the left and right valleys around each peak. The reference valley can
be selected by \code{valley_type}.

\itemize{
\item \code{"higher"}: use the higher of the left and right valleys.
This is the more conservative and standard-like prominence definition.
\item \code{"lower"}: use the lower of the left and right valleys.
This gives a larger prominence value.
}
}
\examples{
x <- seq(1000, 2000, length.out = 2000)
y <- dnorm(x, mean = 1400, sd = 20) * 100 +
     dnorm(x, mean = 1700, sd = 25) * 80
spectrum_data <- data.frame(mz = x, intensity = y)

detected_peaks <- find_peaks_fast(spectrum_data, hws_peaks = 10)

peak_prominence <- calculate_peak_prominence(
  data = spectrum_data,
  peaks = detected_peaks,
  valley_type = "higher"
)

peak_prominence

}
