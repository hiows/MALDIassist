% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_matched_peaks_matrix.R
\name{build_matched_peaks_matrix}
\alias{build_matched_peaks_matrix}
\title{Build a Sample-by-Marker Peak Intensity Matrix}
\usage{
build_matched_peaks_matrix(
  peaks_list,
  reference_mz,
  reference_names = NULL,
  hws_match = 10,
  peak_selection_mode = c("nearest_mz", "maximum_intensity"),
  fill = c("intensity", "log_intensity", "detected", "diff_mz")
)
}
\arguments{
\item{peaks_list}{A named list of peak tables, typically from \code{\link[=find_peaks]{find_peaks()}}.}

\item{reference_mz}{A numeric vector of reference m/z values, for example
from \code{\link[=find_frequent_mz]{find_frequent_mz()}}.}

\item{reference_names}{Optional character vector of marker names.}

\item{hws_match}{Half-window size passed to \code{\link[=match_peaks]{match_peaks()}}. The default is
\code{10}.}

\item{peak_selection_mode}{Peak selection rule passed to \code{\link[=match_peaks]{match_peaks()}}.
The default is \code{"nearest_mz"}.}

\item{fill}{Character string specifying matrix values. One of \code{"intensity"},
\code{"log_intensity"} (\code{log10(pmax(intensity, 1))}), \code{"detected"}, or
\code{"diff_mz"}. For \code{"intensity"}, \code{"log_intensity"}, and \code{"detected"},
unmatched values are filled with \code{0}. For \code{"diff_mz"}, unmatched values
remain \code{NA} so that an exact match (\code{0}) is not confused with a missing
peak.}
}
\value{
A list with elements:
\describe{
\item{matrix}{Numeric matrix with samples as rows and markers as
columns. Column names are \code{reference_names} when supplied, otherwise
character forms of \code{reference_mz}. Columns that are entirely \code{NA}
across samples (no matched peak in any sample) are removed. Remaining
unmatched cells are \code{NA} when \code{fill = "diff_mz"}, otherwise \code{0}.}
\item{matched_matrix}{Logical matrix with the same dimensions as
\code{matrix}. \code{TRUE} indicates a peak was matched within the window.}
\item{reference_mz}{Reference m/z values retained in \code{matrix}.}
\item{reference_names}{Marker names retained in \code{matrix}, if supplied.}
\item{sample_names}{Sample names from \code{peaks_list}.}
\item{matches}{Named list of per-sample \code{\link[=match_peaks]{match_peaks()}} results.}
}
}
\description{
Matches detected peaks from multiple samples to a shared set of reference
m/z values and assembles a sample-by-marker feature matrix.
}
\examples{
peaks_list <- list(
  sample_1 = data.frame(mz = c(1000, 1500, 2000), intensity = c(10, 30, 20)),
  sample_2 = data.frame(mz = c(1002, 2001), intensity = c(12, 25))
)

result <- build_matched_peaks_matrix(
  peaks_list = peaks_list,
  reference_mz = c(1000, 1500, 2000),
  reference_names = c("marker_a", "marker_b", "marker_c"),
  hws_match = 10
)

result$matrix

}
\seealso{
\code{\link[=match_peaks]{match_peaks()}}, \code{\link[=find_frequent_mz]{find_frequent_mz()}}, \code{\link[=heatmap_spectrum_matrix]{heatmap_spectrum_matrix()}}
}
