% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess_maldi_spectra.R
\name{preprocess_maldi_spectra}
\alias{preprocess_maldi_spectra}
\title{Preprocess MALDI-TOF Mass Spectra}
\usage{
preprocess_maldi_spectra(
  spectra,
  hws_sg = 10L,
  pno_sg = 3L,
  baseline_type = c("snip", "tophat"),
  iter_snip = 50L,
  hws_tophat = 50L,
  n_cores = 1L
)
}
\arguments{
\item{spectra}{A two-column \code{data.frame} or numeric \code{matrix} representing
a single spectrum, or a \code{list} of such objects. The first column must
contain strictly increasing m/z values and the second column must contain
intensity values.}

\item{hws_sg}{A positive integer specifying the half-window size used for
Savitzky-Golay smoothing. The full smoothing window size is
\code{2 * hws_sg + 1}.}

\item{pno_sg}{A non-negative integer specifying the polynomial order used
for Savitzky-Golay smoothing. It must be smaller than the full smoothing
window size.}

\item{baseline_type}{A character string specifying the baseline subtraction
method. One of \code{"snip"} or \code{"tophat"}.}

\item{iter_snip}{A positive integer specifying the number of iterations used
for SNIP baseline estimation.}

\item{hws_tophat}{A positive integer specifying the half-window size used
for TopHat baseline estimation.}

\item{n_cores}{Number of worker processes used when \code{spectra} is a list.
The default is \code{1L}, which preserves sequential behaviour.}
}
\value{
A preprocessed spectrum or a list of preprocessed spectra. The
returned object has the same list structure and names as \code{spectra}.
}
\description{
Applies Savitzky-Golay smoothing and baseline subtraction to one or more
MALDI-TOF mass spectra.
}
\details{
Each spectrum is smoothed using \code{\link[=smooth_savitzky_golay]{smooth_savitzky_golay()}} and subsequently
baseline-corrected using \code{\link[=subtract_baseline]{subtract_baseline()}}. Negative values produced
during baseline subtraction are truncated according to
\code{nonnegative_baseline = TRUE}.
}
\examples{
set.seed(1)
spectrum <- data.frame(
  mz = seq(2000, 8000, by = 1),
  intensity = runif(6001)
)

pp_spectrum <- preprocess_maldi_spectra(spectrum)

spectra <- list(sample_1 = spectrum, sample_2 = spectrum)

pp_spectra <- preprocess_maldi_spectra(
  spectra = spectra,
  hws_sg = 10L,
  pno_sg = 3L,
  baseline_type = "snip",
  iter_snip = 50L
)

}
