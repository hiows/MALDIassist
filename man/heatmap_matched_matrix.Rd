% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/heatmap.R
\name{heatmap_matched_matrix}
\alias{heatmap_matched_matrix}
\title{Heatmap of a Sample-by-Marker Matched-Peak Matrix}
\usage{
heatmap_matched_matrix(
  matched_matrix,
  row_cluster = TRUE,
  col_cluster = TRUE,
  group = NULL,
  title = "Matched peaks heatmap",
  center_at_zero = TRUE,
  hide_rownames = FALSE,
  hide_colnames = FALSE
)
}
\arguments{
\item{matched_matrix}{A numeric matrix with samples as rows and markers as
columns, such as the \code{detected_matrix} or \code{delta_mz_matrix} element of
\code{\link[=build_matched_matrix]{build_matched_matrix()}}.}

\item{row_cluster}{Logical indicating whether to cluster rows. The default
is \code{TRUE}.}

\item{col_cluster}{Logical indicating whether to cluster columns. The
default is \code{TRUE}.}

\item{group}{Optional group labels for sample annotation. When supplied,
group colors use a fixed \code{colorspace} Viridis palette.}

\item{title}{Optional plot title.}

\item{center_at_zero}{Logical. When \code{TRUE} (default), uses a diverging
blue-white-red palette with limits symmetric around \code{0}.}

\item{hide_rownames}{Logical. When \code{TRUE}, sample (row) labels are hidden.
The default is \code{FALSE}.}

\item{hide_colnames}{Logical. When \code{TRUE}, marker (column) labels are hidden.
The default is \code{FALSE}.}
}
\value{
A \code{pheatmap} object.
}
\description{
Draws a clustered heatmap of a sample-by-marker matched-peak matrix, such as
the \code{detected_matrix} or \code{delta_mz_matrix} returned by
\code{\link[=build_matched_matrix]{build_matched_matrix()}}, with optional group annotation and a zero-centered
diverging palette. Requires the suggested package \code{pheatmap}.
}
\details{
Missing values (\code{NA}) are shown as grey cells via \code{pheatmap}'s \code{na_col}.
When \code{center_at_zero = TRUE}, finite values are mapped with equal positive
and negative limits so that \code{0} sits at the neutral center color. This is
especially useful for \code{delta_mz_matrix} matrices.
When the matrix contains many \code{NA} values, consider setting
\code{row_cluster = FALSE} or \code{col_cluster = FALSE} because clustering may be
unstable with sparse data.
}
\examples{
if (requireNamespace("pheatmap", quietly = TRUE)) {
  set.seed(1)
  m <- matrix(rnorm(40), nrow = 5)
  rownames(m) <- paste0("sample_", 1:5)
  colnames(m) <- paste0("mz_", 1:8)

  heatmap_matched_matrix(m, title = "Example heatmap")
}

}
\seealso{
\code{\link[=build_matched_matrix]{build_matched_matrix()}}
}
