% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_maldi.R
\name{heatmap_spectrum_matrix}
\alias{heatmap_spectrum_matrix}
\title{Heatmap of a Sample-by-Feature Intensity Matrix}
\usage{
heatmap_spectrum_matrix(
  matrix_obj,
  row_cluster = TRUE,
  col_cluster = TRUE,
  groups = NULL,
  title = "Spectrum heatmap",
  center_at_zero = TRUE
)
}
\arguments{
\item{matrix_obj}{A numeric matrix or output from
\code{\link[=build_matched_peaks_matrix]{build_matched_peaks_matrix()}}.}

\item{row_cluster}{Logical indicating whether to cluster rows. The default
is \code{TRUE}.}

\item{col_cluster}{Logical indicating whether to cluster columns. The
default is \code{TRUE}.}

\item{groups}{Optional group labels for sample annotation. When supplied,
group colors use a fixed \code{colorspace} Viridis palette.}

\item{title}{Optional plot title.}

\item{center_at_zero}{Logical. When \code{TRUE} (default), uses a diverging
blue-white-red palette with limits symmetric around \code{0}.}
}
\value{
A \code{pheatmap} object.
}
\description{
Draws a clustered heatmap of a sample-by-feature intensity matrix, such as
the output of \code{\link[=build_matched_peaks_matrix]{build_matched_peaks_matrix()}}, with optional group annotation
and a zero-centered diverging palette. Requires the suggested package
\code{pheatmap}.
}
\details{
Missing values (\code{NA}) are shown as grey cells via \code{pheatmap}'s \code{na_col}.
When \code{center_at_zero = TRUE}, finite values are mapped with equal positive
and negative limits so that \code{0} sits at the neutral center color. This is
especially useful for \code{fill = "diff_mz"} matrices.
When the matrix contains many \code{NA} values, consider setting
\code{row_cluster = FALSE} or \code{col_cluster = FALSE} because clustering may be
unstable with sparse data.
}
\examples{
if (requireNamespace("pheatmap", quietly = TRUE)) {
  set.seed(1)
  m <- matrix(rnorm(40), nrow = 5)
  rownames(m) <- paste0("sample_", 1:5)
  colnames(m) <- paste0("mz_", 1:8)

  heatmap_spectrum_matrix(m, title = "Example heatmap")
}

}
\seealso{
\code{\link[=build_matched_peaks_matrix]{build_matched_peaks_matrix()}}
}
